# 恢复制药系统到原始状态，仅修复必要参数

## 问题分析
1. **用户需求明确**：只希望修复41.txt的count参数为7，其他制药系统恢复到原始状态
2. **当前状态**：
   - 41.txt中没有`CreateMedicinePot`命令，该命令在42.txt中
   - 之前修改了多个文件，包括`CMD_DatabaseExtension_Medicine.cs`、`MedicinePotManager.cs`、`MedicinePreparingCondition.cs`等
3. **需要恢复的修改**：
   - `CMD_DatabaseExtension_Medicine.cs`：恢复到原始的参数传递逻辑
   - `MedicinePotManager.cs`：恢复到原始的初始化逻辑
   - `MedicinePreparingCondition.cs`：恢复到原始的事件监听注释状态
   - 保留42.txt中count参数从8改为7的修改

## 修复步骤

### 1. 恢复`CMD_DatabaseExtension_Medicine.cs`的原始参数传递
- 将`CreateMedicinePot`方法中的参数传递恢复为原始状态
- 移除我之前添加的注释和修复

### 2. 恢复`MedicinePotManager.cs`的原始初始化逻辑
- 恢复`CreateMedicinePot`方法中的原始代码，移除我的修改

### 3. 恢复`MedicinePreparingCondition.cs`的原始事件监听
- 将事件监听重新注释掉，恢复到原始状态

### 4. 验证42.txt的count参数
- 确保42.txt中的`CreateMedicinePot`命令count参数为7

### 5. 运行诊断工具
- 检查修复后的代码是否有编译错误

## 预期效果
1. 42.txt的count参数正确设置为7，匹配7种目标药品
2. 所有其他制药系统代码恢复到原始状态
3. 熬药功能恢复正常，与原始版本一致
4. 所有关卡的熬药流程正常工作